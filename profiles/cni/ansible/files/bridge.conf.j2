{
    "cniVersion": "{{ cni_version }}",
    "name": "{{ conf_name }}",
    "type": "bridge",
    "bridge": "cnio0",
    "isGateway": true,
    "ipMasq": true,
    "ipam": {
        "type": "host-local",
        "ranges": [
          [{"subnet": "{{ subnet }}"}]
        ],
        "routes": [{"dst": "0.0.0.0/0"}]
    }
}
